name: mateclaw_lints
description: Custom lint rules for Mateclaw Flutter architecture conventions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  analyzer: ^8.0.0
  custom_lint_builder: ^0.8.0
